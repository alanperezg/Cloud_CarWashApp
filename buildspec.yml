version: 0.2

phases: 
    install:
        runtime-versions:
            nodejs: 14
        commands:
            - npm install
    pre_build:
        commands: 
            - echo "we are in the pre build phase"
    build:
        commands:
            - npm run build
            - echo "Built!!!"
    post_build:
        commands:
            - echo "we are in the post build phase"